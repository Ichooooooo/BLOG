# Public production origin used for canonical URLs, Open Graph metadata and the sitemap.
# Cloudflare Pages uses CF_PAGES_URL automatically; set SITE_URL when using a custom domain
# or another host, for example: https://blog.example.com
SITE_URL=https://blog.example.com
