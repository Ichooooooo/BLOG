# Optional custom production origin used for canonical URLs, Open Graph metadata and the sitemap.
# The default is the deployed workers.dev address; set this only after adding a custom domain.
SITE_URL=https://blog.example.com
